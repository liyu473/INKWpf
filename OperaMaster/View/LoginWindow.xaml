<Window
    x:Class="OperaMaster.View.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="600"
    Height="400"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Border
        Background="{DynamicResource RegionBrush}"
        ClipToBounds="True"
        CornerRadius="12"
        Effect="{StaticResource EffectShadow4}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>
                <Border
                    Grid.Column="0"
                    Height="40"
                    HorizontalAlignment="Stretch"
                    Background="Transparent"
                    IsHitTestVisible="True"
                    MouseDown="Border_MouseDown" />
                <Button
                    Grid.Column="1"
                    hc:IconElement.Geometry="{StaticResource CloseGeometry}"
                    BorderThickness="0"
                    Click="CloseButton_Click"
                    Style="{StaticResource ButtonIcon}" />
            </Grid>

            <StackPanel
                Grid.Row="1"
                Margin="0,20,0,20"
                HorizontalAlignment="Center">
                <TextBlock
                    FontSize="28"
                    FontWeight="Bold"
                    Text="OperaMaster" />
                <TextBlock
                    Margin="0,8,0,0"
                    HorizontalAlignment="Center"
                    Foreground="{DynamicResource SecondaryTextBrush}"
                    Text="请登录您的账户" />
            </StackPanel>

            <StackPanel
                Grid.Row="2"
                Margin="40,20"
                VerticalAlignment="Top">
                <hc:TextBox
                    x:Name="UsernameBox"
                    Margin="0,0,0,16"
                    hc:InfoElement.Placeholder="用户名"
                    hc:InfoElement.ShowClearButton="True"
                    hc:InfoElement.Title="用户名:"
                    hc:InfoElement.TitlePlacement="Left"
                    hc:InfoElement.TitleWidth="80" />

                <hc:PasswordBox
                    x:Name="PasswordBox"
                    Margin="0,0,0,24"
                    hc:InfoElement.Placeholder="密码"
                    hc:InfoElement.ShowClearButton="True"
                    hc:InfoElement.Title="密码:"
                    hc:InfoElement.TitlePlacement="Left"
                    hc:InfoElement.TitleWidth="80"
                    ShowEyeButton="True" />

                <CheckBox
                    x:Name="RememberCheckBox"
                    Margin="0,0,0,24"
                    HorizontalAlignment="Right"
                    Content="记住密码" />

                <Button
                    Width="100"
                    Height="40"
                    hc:BorderElement.CornerRadius="20"
                    Click="LoginButton_Click"
                    Content="登 录"
                    Style="{StaticResource ButtonSuccess}" />

                <TextBlock
                    x:Name="ErrorText"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Center"
                    Foreground="{DynamicResource DangerBrush}"
                    Visibility="{Binding Text, RelativeSource={RelativeSource Self}, Converter={StaticResource String2VisibilityConverter}}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
