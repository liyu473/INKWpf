<UserControl
    x:Class="OperaMaster.View.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:viewmodel="clr-namespace:OperaMaster.ViewModel"
    d:DataContext="{d:DesignInstance Type=viewmodel:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <ScrollViewer IsTabStop="False" VerticalScrollBarVisibility="Auto">
        <ikw:SimpleStackPanel Spacing="4">
            <TextBlock
                Margin="0,16,10,5"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="Appearance &amp; behavior" />

            <ui:SettingsCard Description="Select which app theme to display" Header="主题">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Color}" />
                </ui:SettingsCard.HeaderIcon>
                <ComboBox Width="150" SelectedIndex="{Binding SelectedThemeIndex, Mode=TwoWay}">
                    <ComboBoxItem Content="浅色" />
                    <ComboBoxItem Content="深色" />
                    <ComboBoxItem Content="跟随系统" />
                </ComboBox>
            </ui:SettingsCard>

            <ui:SettingsCard Description="Custom app theme color" Header="主题色">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Eyedropper}" />
                </ui:SettingsCard.HeaderIcon>
                <ui:DropDownButton Width="150" Content="修改">
                    <ui:FlyoutService.Flyout>
                        <ui:Flyout>
                            <ikw:SquarePicker
                                MinWidth="200"
                                MinHeight="200"
                                IsEnabled="True"
                                SelectedColor="{Binding SelectedColor, Mode=TwoWay}" />
                        </ui:Flyout>
                    </ui:FlyoutService.Flyout>
                </ui:DropDownButton>
            </ui:SettingsCard>
        </ikw:SimpleStackPanel>
    </ScrollViewer>
</UserControl>
